package Model.Users;

public class Kurir extends User{

    public Kurir(String nama,String alamat,String noHp,String username,String password,String role) {
            super(nama, alamat, noHp, username, password, role);
    }
}
